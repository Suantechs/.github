name: Reusable ping
# Workflow trivial para diagnosticar la resolución de reusable workflows (STP-140).
on:
  workflow_call:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo pong
